import { RightDivDirective } from './right-div.directive';

describe('RightDivDirective', () => {
  it('should create an instance', () => {
    const directive = new RightDivDirective();
    expect(directive).toBeTruthy();
  });
});
