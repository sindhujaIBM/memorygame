import { LeftDivDirective } from './left-div.directive';

describe('LeftDivDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftDivDirective();
    expect(directive).toBeTruthy();
  });
});
